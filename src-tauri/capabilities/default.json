{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal local Markdown reader permissions",
  "windows": ["main"],
  "permissions": ["core:default"]
}
